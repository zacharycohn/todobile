{
  "name": "@todobile/test-utils",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/setup-tests.ts",
  "types": "src/setup-tests.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit false",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config ../../vitest.config.mts --passWithNoTests"
  }
}
